我有两份关于责任标准的json文档，一份是《企业联盟责任标准》，一份是《供应商责任标准》。我的需求是期望逐一对比两个文档中的段落，筛选出其中相近的段落，比如《企业责任联盟》的A分别对应一下《供应商责任标准》中的所有条款。如果不相关直接略过，相关就要将匹配结果保存在对应的excel文档。然后依次完成《企业联盟责任标准》的所有段落针对《供应商责任标准》A的全部匹配，类似于进行了一次遍历。然后再对《供应商责任标准》B条款进行匹配……
两个json结构化的形式是：{元数据；标题；对应段落的原文内容}

目前的实现思路是：
语义向量召回（粗匹配，Top-K）
   ↓
LLM 语义判断（精匹配）
   ↓
规则/置信度过滤
   ↓
导出 Excel

因为全量遍历的效率较低，希望先使用向量相似度先筛Top-K候选，把两个文档所有段落 embedding，建立向量索引（FAISS / Milvus / Chroma）；对企业联盟每个段落：在「供应商责任标准」中召回 Top 3～5，模型使用bge-m3。

紧接着使用LLM 精判（只对 Top-K），判断这两个段落是否在“责任义务层面”对应。调用的模型是 http://10.71.5.24:8000/v1/models，模型名称是gpt-3.5-turbo

置信度显示：保留字段字段向量相似度；LLM 判断结果"不相关，弱相关，强相关"。

导出 Excel：excel文档第一列显示的是企业联盟责任标准中的条款，右边列是匹配出来的《供应商责任标准》中的条款，第三列我希望是相似度匹配得分，第五列是LLM 判断结果。


